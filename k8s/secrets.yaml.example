---
# secrets.yaml - NEVER commit this to git
# Copy from secrets.yaml.example and fill in real values
apiVersion: v1
kind: Secret
metadata:
  name: payment-service-secrets
type: Opaque
stringData:
  database-dsn: "postgres://payment:password@postgres:5432/payments?sslmode=disable"
  # For production (example: AWS RDS)
  # database-dsn: "postgres://payment:SECRET@your-rds.us-east-1.rds.amazonaws.com:5432/payments?sslmode=require"
